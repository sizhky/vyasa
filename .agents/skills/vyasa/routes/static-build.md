# Static Build Route

Read:

- `topics/static-build/usage-output.md`
- `topics/static-build/config-deploy.md`
- `topics/static-build/workflow-notes.md`

Rules:

- Static output must match runtime routes, HTML, and copied assets.
- Host and port config are unused in static mode.
- Test static builds with a local file server when needed.
